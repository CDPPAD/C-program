#include<stdio.h>
int main()
{
	int fahr, celsius;
	int lower, upper, step;
	fahr = lower = 0;
	upper = 260;
	step = 20;
	printf("%d\t%d\n", fahr, upper);
	while(fahr <= upper)
	{
		celsius = 5 * (fahr - 32) / 9;
		printf("%d\t%d\n", fahr, celsius);
		fahr = fahr + step;

	}

	return 0;
}
